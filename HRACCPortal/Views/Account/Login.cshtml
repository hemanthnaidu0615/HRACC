<!--<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(to bottom right, #7F00FF, #E100FF); /* Softer gradient */
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-attachment: fixed;
        }

        .container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            width: 100%;
            padding: 20px;
        }

        .left-content {
            flex: 1;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

            .left-content img {
                max-width: 400px;
                height: auto;
                border-radius: 15px;
                box-shadow: 0 10px 30px rgba(0, 0, 0, 0.25);
                transition: transform 0.5s ease-in-out, opacity 0.5s ease-in-out;
                opacity: 0;
            }

                .left-content img.visible {
                    transform: scale(1.1);
                    opacity: 1;
                }

        .form-container {
            flex: 1;
            background: rgba(255, 255, 255, 0.8); /* Slight transparency */
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.5s ease-out;
        }

            .form-container.visible {
                opacity: 1;
                transform: translateY(0);
            }

        .form-header h2 {
            font-size: 32px;
            font-weight: 600;
            margin-bottom: 10px;
            color: #6a11cb;
            text-align: center;
        }

        .form-header p {
            font-size: 18px;
            color: #5a5a5a;
            text-align: center;
            margin-bottom: 25px;
        }

        .form-group input {
            width: 100%;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 12px;
            margin-bottom: 20px;
            transition: 0.3s;
            font-size: 16px;
        }

            .form-group input:focus {
                border-color: #6a11cb;
                box-shadow: 0 0 8px rgba(106, 17, 203, 0.5);
            }

        .btn-primary {
            width: 100%;
            background: linear-gradient(90deg, #6a11cb, #2575fc);
            border: none;
            border-radius: 30px;
            padding: 12px 20px;
            font-size: 18px;
            font-weight: 600;
            color: #fff;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.3s ease;
        }

            .btn-primary:hover {
                background: linear-gradient(90deg, #2575fc, #6a11cb);
                transform: translateY(-4px);
            }

        .form-footer {
            text-align: center;
            margin-top: 20px;
        }

            .form-footer a {
                color: #6a11cb;
                text-decoration: none;
                font-size: 14px;
                margin: 0 15px;
            }

                .form-footer a:hover {
                    text-decoration: underline;
                }
    </style>
</head>
<body>
    <div class="container">-->
<!-- Left Side: Logo -->
<!--<div class="left-content">
    <img id="logo" src="~/Content/Images/H3U_Logo_Ariba.gif" alt="Company Logo">
</div>-->
<!-- Right Side: Login Form -->
<!--<div class="form-container" id="form">
            <div class="form-header">
                <h2>Welcome to HRACC Portal</h2>
                <p>Fast, Efficient, and Productive</p>
            </div>
            <form method="POST" action="/Account/Login">
                @Html.AntiForgeryToken()
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" placeholder="Enter your email" class="form-control">
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="Enter your password" class="form-control">
                </div>
                <button type="submit" class="btn btn-primary">Sign In</button>
            </form>
            <div class="form-footer">
                <a href="/Account/ForgotPassword">Forgot Password?</a>
                <a href="/Account/Register">Register</a>
            </div>
        </div>
    </div>
    <script>
        // Show animations on load
        document.addEventListener("DOMContentLoaded", () => {
            const logo = document.getElementById('logo');
            const form = document.getElementById('form');

            // Add the visible class to elements
            setTimeout(() => logo.classList.add('visible'), 200);
            setTimeout(() => form.classList.add('visible'), 400);
        });
    </script>
</body>
</html>-->
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600&display=swap" rel="stylesheet">
    <link href="~/Content/login/login.css" rel="stylesheet" />
</head>
<body>
    <div class="container">
        <!-- Left Side: Logo -->
        <div class="left-content">
            <img id="logo" src="~/Content/Images/H3U_Logo_Ariba.gif" alt="Company Logo">
        </div>
        <!-- Right Side: Login Form -->
        <div class="form-container" id="form">
            <div class="form-header">
                <h2>Welcome to HRACC Portal</h2>
                <p>Fast, Efficient, and Productive</p>
            </div>
            <form class="form w-100" novalidate="novalidate" id="kt_sign_in_form"
                  data-kt-redirect-url="@Url.Action("VeiwInvoice", "Invoice")" method="POST" action="/Account/Login">
                @Html.AntiForgeryToken()
                <span class="text-danger" id="login-error">@Html.ValidationSummary("", new { @class = "text-danger" })</span>
                <input type="hidden" name="returnUrl" value="@ViewBag.ReturnUrl" />
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" placeholder="Enter your email" class="form-control" required>
                    <span class="text-danger" id="email-error"></span> <!-- Email error message -->
                </div>
                <div class="form-group">

                    <label for="password">Password</label>
                    <div class="input-group">
                        <input type="password" id="password" name="password" placeholder="Enter your password" class="form-control" required>
                        <div class="input-group-append">
                            <span class="input-group-text" onclick="togglePassword()" style="cursor: pointer;">
                                <i id="toggleIcon" class="fa fa-eye"></i>
                            </span>
                        </div>
                        <!-- Password error message -->
                    </div>
                    <span class="text-danger" id="password-error"></span>
                </div>
                <span class="text-danger" id="login-error"></span> <!-- Display login errors -->
                <button type="submit" id="kt_sign_in_submit" class="btn btn-primary">Sign In</button>
            </form>
            <div class="form-footer">
                <a href="/Account/ResetPasswordModel">Reset Password?</a>
                <a href="/Account/Register">Register</a>
            </div>
        </div>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
    <script>
        // Show animations on load
        document.addEventListener("DOMContentLoaded", () => {
            const logo = document.getElementById('logo');
            const form = document.getElementById('form');

            // Add the visible class to elements
            setTimeout(() => logo.classList.add('visible'), 200);
            setTimeout(() => form.classList.add('visible'), 400);
        });
        function togglePassword() {
            console.log("Loaded")
            const passwordInput = document.getElementById('password');
            const toggleIcon = document.getElementById('toggleIcon');
            if (passwordInput.type === "password") {
                passwordInput.type = "text";
                toggleIcon.classList.remove("fa-eye");
                toggleIcon.classList.add("fa-eye-slash");
            } else {
                passwordInput.type = "password";
                toggleIcon.classList.remove("fa-eye-slash");
                toggleIcon.classList.add("fa-eye");
            }
        }


        document.getElementById("kt_sign_in_form").addEventListener("submit", function (event) {
            let valid = true;
            console.log("Loaded")
            document.getElementById("email-error").textContent = "";
            document.getElementById("password-error").textContent = "";
            document.getElementById("login-error").textContent = "";

            if (!document.getElementById("email").value) {
                document.getElementById("email-error").textContent = "Email is required.";
                valid = false;
            }
            if (!document.getElementById("password").value) {
                document.getElementById("password-error").textContent = "Password is required.";
                valid = false;
            }

            if (!valid) {
                event.preventDefault(); // Stop form submission
            }
        });
    </script>
</body>
</html>